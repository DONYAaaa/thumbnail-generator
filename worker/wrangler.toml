name = "thumbnail-generator-worker"
main = "index.js"
compatibility_date = "2026-03-24"

# Widget is served from the same origin as the API so the UI can GET /config (values from .dev.vars / dashboard).
[assets]
directory = "../widget"
binding = "ASSETS"
run_worker_first = true
